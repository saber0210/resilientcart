apiVersion: 1

providers:
  - name: ResilientCart
    orgId: 1
    folder: ResilientCart
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
